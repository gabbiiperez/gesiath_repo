<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista de Formulario -->
    <record id="library_libraries_view_form" model="ir.ui.view">
        <field name="name">library.libraries.view.form</field>
        <field name="model">library.libraries</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="address"/>
                        <field name="phone" widget="phone"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <!-- Vista de Lista -->
    <record id="library_libraries_view_tree" model="ir.ui.view">
        <field name="name">library.libraries.view.tree</field>
        <field name="model">library.libraries</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="address"/>
                <field name="phone" widget="phone"/>
            </tree>
        </field>
    </record>
</odoo>
